---
name: "state_overview"
description: "Get an overview of Service states via ChatOps"
action_ref: "livestatus.state_overview"
formats:
  - "shinken service overview"
ack:
  enabled: false
  format: "The command has been seen and is being processed.  The result will be posted as soon as possible"
result:
  enabled: true
  format: "Standard out says {{ execution.result.result }}"
