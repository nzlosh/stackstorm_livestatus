---
    chain:
        -
            name: "lookup_host"
            ref: "livestatus.get"
            parameters:
                table: hosts
                columns:
                  - address
                filters:
                  - host_name = {{host}}
    default: "lookup_host"
