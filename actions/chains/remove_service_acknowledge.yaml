---
  chain:
    -
      name: "contact_email"
      ref: "livestatus.exec_cmd"
      parameters:
        table: contact
        columns:
          - email
        filters:
          - name = {{name}}
  default: "contact_email"
        'REMOVE_SVC_ACKNOWLEDGEMENT': {'global': False, 'args': ['service']},
